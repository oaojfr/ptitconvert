# PtitConvert - Dépendances Python
# Convertisseur de fichiers avancé avec interface graphique

# === CORE LIBRARIES ===
Pillow>=9.0.0          # Manipulation d'images (PNG, JPG, etc.)
PyPDF2>=3.0.0          # Traitement et manipulation des PDF
python-docx>=1.0.0     # Documents Microsoft Word (.docx)
openpyxl>=3.1.0        # Feuilles de calcul Excel (.xlsx)
reportlab>=4.0.0       # Génération de PDF
img2pdf>=0.4.0         # Conversion d'images vers PDF
pandas>=1.3.0          # Manipulation de données et CSV

# === FORMATS AVANCÉS ===
ebooklib>=0.18         # Support des eBooks (EPUB)
odfpy>=1.4.0          # Documents OpenDocument (ODT, ODS)
rarfile>=4.0          # Extraction d'archives RAR
py7zr>=0.20.0         # Support des archives 7Z

# === AUDIO/VIDEO ===
moviepy>=1.0.3        # Conversion vidéo (MP4, AVI, etc.)
pydub>=0.25.1         # Manipulation audio (MP3, WAV, FLAC)

# === INTERFACE CLI ===
click>=8.0.0          # Framework CLI moderne
colorama>=0.4.0       # Couleurs dans le terminal

# === INTERFACE GRAPHIQUE ===
# tkinter est inclus avec Python par défaut
